<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Confirm Your Account</title>
  <style type="text/css">
    body {
      font-family: Arial, sans-serif;
      background-color: #fdfdfd;
      padding: 20px;
    }
    .container {
      max-width: 700px;
      margin: 0 auto;
      background-color: #fbf7f6;
      border-radius: 8px;
      padding: 40px;
    }
    .header {
      text-align: center;
      margin-bottom: 30px;
    }
    .header h1 {
      font-size: 24px;
      font-weight: 300;
      color: #462c34;
    }
    .info {
      width: 300px;
      margin: 0 auto;
      border: 1px solid #aba8a7;
      border-radius: 8px;
      padding: 0 20px 20px;
      margin-bottom: 40px;
    }
    p {
      font-size: 16px;
      color: rgb(125, 118, 115);
    }
    .soft {
      margin-top: 20px;
      margin-bottom: 5px;
      font-size: 14px;
      color: #aba8a7;
    }
    .btn-container {
      text-align: center;
      margin-top: 30px;
    }
    .btn {
      display: inline-block;
      padding: 10px 20px;
      background-color: #4e7be4;
      color: #fff;
      text-decoration: none;
      border-radius: 5px;
      font-size: 16px;
    }
    .btn:hover {
      background-color: #89a9ec;
    }
    .logo-container {
      height: 50px;
      display: flex;
      color: #462c34;
      align-items: center;
      justify-content: center;
      gap: 13px;
      margin-bottom: 2rem;
    }
    .logo-container h2 {
      font-weight: 300;
      font-size: 16px;
    }
    .logo {
      height: 33px;
      filter: brightness(0.25);
    }
  </style>
</head>
<body width="100%">
  <div class="container">
    <div class="header">
      <div class="logo-container">
        <%= email_inline_image_tag("header/logo-round.png",
            class: "logo",
            alt: "ETM Logo") %>
        <h2><%=I18n.t('meta.title')%></h2>
      </div>
      <h1>You just created a new personal access token for the Energy Transition Model.</h1>
    </div>
    <div class="info">
      <p style="display:flex;flex-direction:column">
        <span class="soft">Token name</span>
        <%= @token.name %>
        <span class="soft">Expires at</span>
        <%= l(@access_token.expires_at, format: :long) %>
        <span class="soft">Token permissions</span>
        <%= token_permissions(@access_token).html_safe %>
      </p>
    </div>
    <div class="header">
      <p>
        This token can be used to access your account using the API.
        If you did not create this token, please visit the following link to revoke it.
      </p>
    </div>
    <div class="btn-container">
      <a href="<%= identity_tokens_url %>" class="btn"> View my tokens</a>
    </div>
  </div>
</body>
</html>
