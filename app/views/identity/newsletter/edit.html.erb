<%= turbo_frame_tag @audience do %>
  <%= render(Identity::NewsletterStatusRowComponent.new(
    subscribed: MyEtm::Mailchimp.subscribed?(current_user.email, audience: @audience), audience: @audience
  )) %>
<% end %>
