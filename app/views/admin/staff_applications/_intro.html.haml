%div.intro-container.bg-blue-50.border-l-4.border-blue-400.p-4.rounded-lg.mb-6.max-w-2xl
  %p.text-sm.text-gray-700.mb-2
    To set up your local environment, press 'create' and copy the config:

  %ul.text-sm.text-gray-700.list-disc.ml-5.mb-2
    %li For ETEngine and ETModel, paste it into 'settings.local.yml'.
    %li For Collections, paste it into '.env.local'.

  %p.text-sm.text-gray-700
    If these files don’t exist, create them in the same location as 'settings.yml' and '.env'.
  %p.text-sm.text-gray-700
    Restart your local applications after making these changes.
