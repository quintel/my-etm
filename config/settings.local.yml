# Local development configuration overrides
# This file is gitignored and safe for local secrets

# Sentry Configuration (uses production DSN, filtered by environment:development tag)
# MyETM uses US-based Sentry ingest (different from ETEngine/ETModel)
sentry_dsn: "https://60adefe39e63b5ed6dd418b3fc8b16ca@o187050.ingest.us.sentry.io/4508620397871104"

# Sentry Sampling: 100% for development testing (ensures every request is traced)
sentry_traces: "1.0"
sentry_profiles: "1.0"
